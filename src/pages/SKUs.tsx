import React from 'react';
import SKUManager from '../component/SKUManager';

const SKUs: React.FC = () => {
    return (
        <>
            <SKUManager/>
        </>
    );
};

export default SKUs;